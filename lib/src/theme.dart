import 'package:flutter/material.dart';

final ThemeData trackerKnightsTheme = ThemeData.from(
    colorScheme: ColorScheme.fromSwatch(
        primarySwatch: Colors.blueGrey,
        accentColor: Colors.lightBlueAccent.shade200));
